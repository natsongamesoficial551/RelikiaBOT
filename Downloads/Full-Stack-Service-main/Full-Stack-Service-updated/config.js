// config.js — Gerado automaticamente pelo Netlify Build
// As variáveis abaixo são injetadas pelo Netlify em tempo de build.
// NUNCA coloque valores hardcoded aqui. Configure no painel do Netlify:
// Site Settings → Environment Variables

window.APP_CONFIG = {
  SUPABASE_URL:      "%%SUPABASE_URL%%",
  SUPABASE_ANON_KEY: "%%SUPABASE_ANON_KEY%%"
};
